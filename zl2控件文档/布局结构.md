# ZL2 控件系统 - 布局结构文档

> 来源: `e:\project\ZalithLauncher2\LayerController\src\main\java\com\movtery\layer_controller\layout\`

## 1. ControlLayout (整体布局)

```kotlin
@Serializable
data class ControlLayout(
    @SerialName("info") val info: Info,                    // 基本信息
    @SerialName("layers") val layers: List<ControlLayer>,  // 控件图层列表
    @SerialName("styles") val styles: List<ButtonStyle>,    // 按钮样式列表
    @SerialName("special") val special: Special,            // 特殊配置
    @SerialName("editorVersion") val editorVersion: Int     // 编辑器版本
)
```

### Info (布局基本信息)

```kotlin
data class Info(
    @SerialName("name") val name: TranslatableString,       // 布局名称
    @SerialName("author") val author: TranslatableString,  // 作者
    @SerialName("description") val description: TranslatableString, // 描述
    @SerialName("versionCode") val versionCode: Int,        // 版本代码
    @SerialName("versionName") val versionName: String      // 版本名称
)
```

### Special (特殊配置)

```kotlin
data class Special(
    val joystickStyle: JoystickStyle? = null  // 摇杆样式（可选）
)
```

## 2. ControlLayer (控件图层)

```kotlin
@Serializable
data class ControlLayer(
    @SerialName("name") val name: String,                    // 图层名称
    @SerialName("uuid") val uuid: String,                   // 图层唯一标识
    @SerialName("hide") val hide: Boolean,                   // 默认隐藏
    @SerialName("hideWhenMouse") val hideWhenMouse: Boolean,  // 鼠标操作后隐藏
    @SerialName("hideWhenGamepad") val hideWhenGamepad: Boolean, // 手柄操作后隐藏
    @SerialName("hideWhenJoystick") val hideWhenJoystick: Boolean, // 使用摇杆时隐藏
    @SerialName("visibilityType") val visibilityType: VisibilityType, // 可见场景
    @SerialName("normalButtons") val normalButtons: List<NormalData>, // 普通按钮列表
    @SerialName("textBoxes") val textBoxes: List<TextData>   // 文本框列表
)
```

### HideLayerWhen 隐藏条件

图层支持多种隐藏条件：

| 条件 | 说明 |
|------|------|
| `hideWhenMouse` | 实体鼠标操作时隐藏 |
| `hideWhenGamepad` | 手柄操作时隐藏 |
| `hideWhenJoystick` | 使用虚拟摇杆时隐藏 |

### VisibilityType 可见场景

| 值 | 说明 |
|---|---|
| `always` | 始终展示 |
| `in_game` | 虚拟鼠标捕获时展示 |
| `in_menu` | 虚拟鼠标释放时展示 |

## 3. 版本迁移 (Version.kt)

```kotlin
internal const val EDITOR_VERSION = 11
```

### 版本历史

| 版本 | 变更 |
|------|------|
| 1 → 2 | 控件位置、大小精确到小数点后 2 位 |
| 2 → 3 | 支持鼠标/手柄操作后隐藏图层 |
| 3 → 4 | 支持文本对齐、粗体、斜体、下划线 |
| 4 → 10 | 支持点击事件、文本大小、摇杆样式等 |
| 10 → 11 | 控件外观支持不区分系统主题 |

### 迁移函数

```kotlin
internal fun updateLayoutToNew(layout: ControlLayout): ControlLayout {
    return when (layout.editorVersion) {
        1 -> updateLayoutToNew(update1To2(layout))
        2 -> updateLayoutToNew(update2To3(layout))
        3 -> updateLayoutToNew(update3To4(layout))
        4, 5, 6, 7, 8, 9 -> updateLayoutToNew(update4To10(layout))
        10 -> updateLayoutToNew(update10To11(layout))
        else -> layout
    }
}
```

## 4. 布局加载与保存

### 从文件加载

```kotlin
fun loadLayoutFromFile(file: File): ControlLayout {
    val jsonString = file.readText()
    return loadLayoutFromString(jsonString)
}
```

### 从字符串加载

```kotlin
fun loadLayoutFromString(jsonString: String): ControlLayout {
    val jsonObject = layoutJson.decodeFromString<JsonObject>(jsonString)
    if (jsonObject["editorVersion"] == null)
        throw SerializationException("The file does not contain the key \"editorVersion\".")
    val version = jsonObject["editorVersion"]!!.jsonPrimitive.int
    if (version <= EDITOR_VERSION) {
        var layout = layoutJson.decodeFromString<ControlLayout>(jsonString)
        if (version < EDITOR_VERSION) layout = updateLayoutToNew(layout)
        return layout
    } else {
        throw IllegalArgumentException("Control layout versions are not supported!")
    }
}
```

### 保存到文件

```kotlin
suspend fun ControlLayout.saveToFile(file: File) {
    withContext(Dispatchers.IO) {
        val jsonString = layoutJson.encodeToString(this@saveToFile)
        file.writeText(jsonString)
    }
}
```

## 5. JSON 序列化配置

```kotlin
internal val layoutJson = Json {
    prettyPrint = true
    ignoreUnknownKeys = true
    serializersModule = SerializersModule {
        contextual(Color::class, ColorSerializer)
    }
}
```

## 6. 图层创建

```kotlin
fun createNewLayer(defaultLayerName: String = ""): ControlLayer {
    return ControlLayer(
        name = defaultLayerName,
        uuid = randomUUID(),
        hide = false,
        hideWhenMouse = true,
        hideWhenGamepad = true,
        visibilityType = VisibilityType.ALWAYS
    )
}
```

## 7. 布局示例结构

```json
{
    "info": {
        "name": { "default": "default", "matchQueue": [] },
        "author": { "default": "MovTery", "matchQueue": [] },
        "description": { "default": "", "matchQueue": [] },
        "versionCode": 0,
        "versionName": "1.0"
    },
    "layers": [
        {
            "name": "gui",
            "uuid": "6df122c8cbf3",
            "hide": false,
            "visibilityType": "always",
            "normalButtons": [...],
            "textBoxes": [...]
        }
    ],
    "styles": [
        {
            "name": "default",
            "uuid": "d1096cf91caa",
            "animateSwap": false,
            "lightStyle": {...},
            "darkStyle": {...}
        }
    ],
    "special": {
        "joystickStyle": null
    },
    "editorVersion": 10
}
```

## 8. 事件处理

```kotlin
class EventHandler(
    private val handle: (event: ClickEvent, pressed: Boolean) -> Unit = { _, _ -> }
) {
    internal fun onKeyPressed(
        clickEvents: List<ClickEvent>,
        isPressed: Boolean,
        handle: (ClickEvent) -> Boolean = { true }
    )

    internal fun onSwitchLayer(
        clickEvent: ClickEvent,
        allLayers: List<ObservableControlLayer>,
        switch: (ObservableControlLayer) -> Unit,
        show: (ObservableControlLayer) -> Unit,
        hide: (ObservableControlLayer) -> Unit
    )
}
```

## 9. UUID 生成

```kotlin
internal fun randomUUID(length: Int = 12): String =
    UUID.randomUUID()
        .toString()
        .replace("-", "")
        .take(length)

internal fun getAButtonUUID() = randomUUID(18)
```

- 普通 UUID: 12 位随机字符串
- 按钮 UUID: 18 位随机字符串

## 10. 控件克隆

```kotlin
fun NormalData.cloneNew(): NormalData = NormalData(
    text = this.text,
    uuid = getAButtonUUID(),
    position = CenterPosition,  // 位置重置为中心
    buttonSize = buttonSize,
    buttonStyle = buttonStyle,
    textAlignment = textAlignment,
    textBold = textBold,
    textItalic = textItalic,
    textUnderline = textUnderline,
    visibilityType = visibilityType,
    _clickEvents = clickEvents,
    isSwipple = isSwipple,
    isPenetrable = isPenetrable,
    isToggleable = isToggleable
)
```

> 克隆时 UUID 和位置会生成新的。
