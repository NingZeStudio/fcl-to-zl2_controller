# 更新日志

## v1.0.1 (2024-12-18) - 修复颜色值精度问题

### 🐛 Bug 修复

**问题**: 导入 ZL2 时出现 "Numeric value overflow" 错误

**原因**: JavaScript 的 Number 类型无法精确表示 ZL2 的大整数颜色值（如 `-9223372036854775808`），导致 JSON 序列化时精度丢失。

**解决方案**:
1. 在 TypeScript 类型中将颜色值改为 `string` 类型
2. 在 JSON 输出时使用正则表达式移除引号，转换为纯数字
3. 确保输出的 JSON 格式符合 ZL2 要求

### 📝 修改的文件

- `src/types/zl2.ts` - 颜色值类型改为 string
- `src/converter/keymap.ts` - 颜色常量改为字符串
- `src/App.vue` - 添加 JSON 格式转换逻辑

### ✅ 修复效果

**修复前**:
```json
{
  "backgroundColor": -9223372036854776000  // ❌ 精度丢失
}
```

**修复后**:
```json
{
  "backgroundColor": -9223372036854775808  // ✅ 精确值
}
```

### 🧪 测试方法

1. 刷新浏览器页面（Ctrl + F5）
2. 点击"加载示例"按钮
3. 点击"开始转换"按钮
4. 检查输出 JSON 中的颜色值：
   - ✅ 应该是数字（无引号）
   - ✅ 应该是完整的值（无精度丢失）
5. 复制 JSON 并在 ZL2 中导入
6. 确认导入成功，无错误提示

### 📋 技术细节

#### JavaScript Number 的限制

JavaScript 使用 IEEE 754 双精度浮点数表示数字：
- 安全整数范围: `-(2^53 - 1)` 到 `2^53 - 1`
- 即: `-9007199254740991` 到 `9007199254740991`

ZL2 的颜色值超出了这个范围：
- `-9223372036854775808` (超出安全范围)
- `-5510004026390872064` (超出安全范围)

#### 解决方案原理

1. **存储阶段**: 使用字符串存储大整数
   ```typescript
   const SAFE_ZL2_COLORS = {
     TRANSPARENT_BLACK: '-9223372036854775808'  // 字符串
   }
   ```

2. **序列化阶段**: JSON.stringify 会将字符串加上引号
   ```json
   "backgroundColor": "-9223372036854775808"
   ```

3. **转换阶段**: 使用正则表达式移除引号
   ```typescript
   jsonStr.replace(/"(-?\d{10,})"/g, '$1')
   ```
   
   结果:
   ```json
   "backgroundColor": -9223372036854775808
   ```

### 🔍 验证清单

使用以下清单验证修复是否成功：

- [ ] 浏览器页面已刷新
- [ ] 转换功能正常工作
- [ ] 输出 JSON 中颜色值为数字（无引号）
- [ ] 颜色值精确（无精度丢失）
- [ ] ZL2 能够成功导入
- [ ] 导入后控件显示正常
- [ ] 无 "Numeric value overflow" 错误

### 💡 使用建议

1. **清除浏览器缓存**: 如果问题仍然存在，请清除浏览器缓存或使用无痕模式
2. **检查输出**: 在复制 JSON 前，先检查颜色值格式是否正确
3. **保存配置**: 转换成功后，建议保存 JSON 文件作为备份

---

## v1.0.0 (2024-12-18) - 初始版本

### ✨ 新功能

- ✅ 完整的 FCL 到 ZL2 转换功能
- ✅ 50+ 键码自动映射
- ✅ 方向键转 8 按钮
- ✅ 安全颜色值处理
- ✅ 响应式 UI 设计
- ✅ 一键复制/下载
- ✅ 转换统计显示
- ✅ 完善的文档系统

---

## 🔮 未来计划

### v1.1.0 (计划中)
- [ ] 添加单元测试
- [ ] 支持批量转换
- [ ] 添加转换历史
- [ ] 配置预览功能

### v1.2.0 (计划中)
- [ ] ZL2 到 FCL 反向转换
- [ ] 可视化编辑器
- [ ] 配置对比功能

---

**最后更新**: 2024-12-18  
**当前版本**: v1.0.1
