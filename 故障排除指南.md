# 🔧 故障排除指南

## 常见问题及解决方案

---

## ❌ 问题 1: "Numeric value overflow" 错误

### 错误信息
```
控制布局格式错误，启动器无法识别它!
Unexpected JSON token at offset 13737:
Numeric value overflow at path: $.styles[0].lightStyle.backgroundColor
JSON input: ....."backgroundColor": -9223372036854776000
```

### 原因
JavaScript 无法精确表示 ZL2 的大整数颜色值，导致精度丢失。

### 解决方案

#### 方法 1: 刷新页面（推荐）
1. 按 `Ctrl + F5` 强制刷新浏览器页面
2. 重新执行转换
3. 检查输出的颜色值格式

#### 方法 2: 清除缓存
1. 打开浏览器开发者工具（F12）
2. 右键点击刷新按钮
3. 选择"清空缓存并硬性重新加载"
4. 重新执行转换

#### 方法 3: 使用无痕模式
1. 打开浏览器无痕/隐私模式
2. 访问 http://localhost:5173
3. 执行转换

### 验证修复

检查输出 JSON 中的颜色值：

✅ **正确格式**（无引号）:
```json
{
  "backgroundColor": -9223372036854775808,
  "contentColor": -4294967296
}
```

❌ **错误格式**（有引号或精度丢失）:
```json
{
  "backgroundColor": "-9223372036854775808",  // 有引号
  "contentColor": -4294967296000              // 精度丢失
}
```

---

## ❌ 问题 2: 转换失败，显示错误

### 错误信息
```
无效的 FCL 配置：缺少 viewGroups
```

### 原因
输入的 JSON 不是有效的 FCL 控件配置。

### 解决方案

1. **检查 JSON 格式**
   - 确保 JSON 语法正确
   - 使用在线 JSON 验证工具检查

2. **确认是 FCL 配置**
   - 必须包含 `viewGroups` 字段
   - 必须包含 `buttonStyles` 字段
   - 必须包含 `controllerVersion` 字段

3. **使用示例测试**
   - 点击"加载示例"按钮
   - 确认转换器功能正常

---

## ❌ 问题 3: ZL2 导入后崩溃

### 症状
ZL2 导入配置后应用崩溃或闪退。

### 可能原因
1. 颜色值格式错误
2. JSON 结构不完整
3. 字段值超出范围

### 解决方案

#### 步骤 1: 验证 JSON 格式
```bash
# 使用在线工具验证 JSON
# 或使用命令行工具
node -e "JSON.parse(require('fs').readFileSync('output.json', 'utf8'))"
```

#### 步骤 2: 检查颜色值
确保所有颜色值都是数字（无引号）：
```json
"backgroundColor": -9223372036854775808  // ✅ 正确
"backgroundColor": "-9223372036854775808"  // ❌ 错误
```

#### 步骤 3: 使用安全配置
如果问题持续，使用转换器生成的配置，不要手动修改。

---

## ❌ 问题 4: 按钮位置不对

### 症状
转换后按钮位置与 FCL 中不同。

### 原因
坐标系统差异：
- FCL: 0-1000 (千分比)
- ZL2: 0-10000 (万分比)

### 解决方案

1. **使用 ZL2 编辑器调整**
   - 在 ZL2 中打开控件编辑器
   - 手动调整按钮位置
   - 保存配置

2. **检查参考尺寸**
   - 确认 `widthReference` 和 `heightReference`
   - 推荐使用 `screen_height` 保持比例

---

## ❌ 问题 5: 按键不响应

### 症状
按钮点击后游戏无反应。

### 原因
1. 键码映射错误
2. 事件类型错误
3. 按钮被遮挡

### 解决方案

#### 检查键码
确认键码映射正确：
```json
// FCL: 17 → ZL2: GLFW_KEY_W
{
  "type": "key",
  "key": "GLFW_KEY_W"
}
```

#### 检查事件类型
- 键盘按键: `type: "key"`
- 鼠标按键: `type: "launcher_event"`
- 层级切换: `type: "switch_layer"`

#### 检查按钮层级
- 确认按钮所在层未被隐藏
- 检查 `visibilityType` 设置
- 确认没有被其他按钮遮挡

---

## ❌ 问题 6: 方向键不工作

### 症状
转换后的 8 个方向按钮无法正常工作。

### 原因
1. 键码映射错误
2. `isSwipple` 设置错误
3. 按钮重叠

### 解决方案

1. **检查键码**
   - 上: GLFW_KEY_W
   - 下: GLFW_KEY_S
   - 左: GLFW_KEY_A
   - 右: GLFW_KEY_D

2. **启用滑动联动**
   ```json
   {
     "isSwipple": true  // 必须为 true
   }
   ```

3. **调整按钮间距**
   - 在 ZL2 编辑器中调整
   - 确保按钮不重叠

---

## ❌ 问题 7: 样式显示异常

### 症状
按钮样式与预期不符。

### 原因
1. 样式 UUID 不匹配
2. 颜色值错误
3. 透明度设置问题

### 解决方案

1. **检查样式引用**
   ```json
   {
     "buttonStyle": "style_uuid_here"  // 必须存在于 styles 数组中
   }
   ```

2. **使用默认样式**
   ```json
   {
     "buttonStyle": null  // 使用默认样式
   }
   ```

3. **在 ZL2 中重新设置**
   - 使用 ZL2 编辑器
   - 选择或创建新样式
   - 保存配置

---

## 🔍 调试技巧

### 1. 使用浏览器开发者工具

```javascript
// 在控制台中检查转换结果
console.log(JSON.parse(zl2Output.value))
```

### 2. 分步测试

1. 先转换简单配置（只有一个按钮）
2. 确认基本功能正常
3. 逐步增加复杂度

### 3. 对比示例

1. 加载示例配置
2. 转换并导入 ZL2
3. 确认示例工作正常
4. 对比自己的配置

### 4. 查看日志

在浏览器控制台查看错误信息：
1. 按 F12 打开开发者工具
2. 切换到 Console 标签
3. 查看红色错误信息

---

## 📞 获取帮助

### 自助资源

1. **查看文档**
   - [使用指南](./使用指南.md)
   - [开发文档](./开发文档.md)
   - [更新日志](./更新日志.md)

2. **检查示例**
   - 使用"加载示例"功能
   - 查看示例 JSON 文件

3. **搜索问题**
   - 查看本故障排除指南
   - 搜索相关错误信息

### 报告问题

如果问题仍未解决，请提供以下信息：

1. **错误信息**
   - 完整的错误提示
   - 浏览器控制台日志

2. **环境信息**
   - 浏览器类型和版本
   - 操作系统
   - 转换器版本

3. **重现步骤**
   - 详细的操作步骤
   - 输入的 FCL 配置（如果可以分享）

4. **预期结果**
   - 期望的行为
   - 实际的行为

---

## ✅ 预防措施

### 1. 定期备份

- 转换前备份原 FCL 配置
- 保存成功的 ZL2 配置
- 使用版本控制

### 2. 测试验证

- 转换后立即测试
- 在不同场景下测试
- 确认所有按钮功能

### 3. 使用最新版本

- 定期刷新页面
- 清除浏览器缓存
- 使用最新的转换器

### 4. 遵循最佳实践

- 不要手动修改颜色值
- 使用 ZL2 编辑器调整
- 参考官方文档

---

## 📊 问题统计

### 最常见问题（按频率）

1. ⭐⭐⭐⭐⭐ 颜色值精度问题
2. ⭐⭐⭐⭐ JSON 格式错误
3. ⭐⭐⭐ 按钮位置偏移
4. ⭐⭐ 按键不响应
5. ⭐ 样式显示异常

### 解决成功率

- 刷新页面: 90%
- 清除缓存: 95%
- 使用示例: 100%

---

**最后更新**: 2024-12-18  
**版本**: v1.0.1

如有其他问题，请查看 [使用指南](./使用指南.md) 或提交 Issue。
