# FCL 到 ZL2 控件转换器 - 使用指南

## 🚀 快速开始

### 1. 启动应用

```bash
# 安装依赖（首次运行）
npm install

# 启动开发服务器
npm run dev
```

访问 http://localhost:5173

### 2. 转换步骤

1. **准备 FCL 配置**
   - 从 FCL 启动器导出控件配置
   - 或使用示例配置（点击"加载示例"按钮）

2. **粘贴配置**
   - 将 FCL JSON 配置粘贴到左侧输入框

3. **执行转换**
   - 点击"开始转换"按钮
   - 等待转换完成

4. **获取结果**
   - 在右侧查看转换后的 ZL2 配置
   - 点击"复制结果"或"下载 JSON"

5. **导入 ZL2**
   - 在 ZL2 启动器中选择"导入控件布局"
   - 选择下载的 JSON 文件

## 📋 转换说明

### 控件类型转换

#### 普通按钮
- ✅ 文本显示
- ✅ 位置和大小
- ✅ 按键事件
- ✅ 样式配置
- ✅ 可见性设置

#### 方向键
FCL 的方向键会被转换为 8 个独立按钮：

```
◤  ▲  ◥
◀  ○  ▶
◣  ▼  ◢
```

每个按钮支持：
- 单方向移动（上下左右）
- 斜向移动（组合键）
- 滑动联动（isSwipple: true）

### 键码映射表

| FCL 键码 | ZL2 键码 | 说明 |
|---------|---------|------|
| 17 | GLFW_KEY_W | W键 |
| 31 | GLFW_KEY_S | S键 |
| 29 | GLFW_KEY_A | A键 |
| 32 | GLFW_KEY_D | D键 |
| 57 | GLFW_KEY_SPACE | 空格 |
| 42 | GLFW_KEY_LEFT_SHIFT | 左Shift |
| 18 | GLFW_KEY_E | E键 |
| 1000 | GLFW_MOUSE_BUTTON_LEFT | 鼠标左键 |
| 1002 | GLFW_MOUSE_BUTTON_RIGHT | 鼠标右键 |
| 1003 | launcher.event.scroll_up | 滚轮向上 |
| 1004 | launcher.event.scroll_down | 滚轮向下 |

完整映射见 `src/converter/keymap.ts`

### 坐标系统转换

#### FCL 坐标系统
- 范围: 0-1000
- 单位: 千分比
- 示例: 500 = 50%

#### ZL2 坐标系统
- 范围: 0-10000
- 单位: 万分比
- 示例: 5000 = 50%

#### 转换公式
```typescript
ZL2坐标 = FCL坐标 × 10
```

### 尺寸系统转换

#### FCL 尺寸
```json
{
  "percentageWidth": {
    "reference": "SCREEN_WIDTH",
    "size": 50  // 5%
  }
}
```

#### ZL2 尺寸
```json
{
  "widthPercentage": 500,  // 5%
  "widthReference": "screen_width"
}
```

#### 转换公式
```typescript
ZL2尺寸 = FCL尺寸 × 10
```

### 样式转换

#### 颜色值处理
为避免 ZL2 崩溃，转换器使用安全的颜色值：

```typescript
// 安全颜色值
const SAFE_COLORS = {
  TRANSPARENT_BLACK: -9223372036854775808,  // 半透明黑色背景
  GRAY: -5510004026390872064,               // 灰色（按下状态）
  WHITE: -4294967296                        // 白色（文字/边框）
}
```

⚠️ **重要**: 不要手动修改这些颜色值！如需自定义颜色，请使用 ZL2 编辑器。

#### 透明度计算
```typescript
// 从 FCL 颜色中提取 alpha 通道
alpha = (color >>> 24) & 0xFF / 255
```

#### 圆角转换
```typescript
// FCL: 100 = 10dp
// ZL2: 10.0 = 10dp
ZL2圆角 = FCL圆角 / 10
```

## 🎯 高级功能

### 事件转换

#### 按键事件
```json
// FCL
{
  "pressEvent": {
    "autoKeep": true,
    "outputKeycodes": [17]  // W键
  }
}

// ZL2
{
  "clickEvents": [
    {
      "type": "key",
      "key": "GLFW_KEY_W"
    }
  ]
}
```

#### 组合键
```json
// FCL
{
  "outputKeycodes": [42, 17]  // Shift + W
}

// ZL2
{
  "clickEvents": [
    { "type": "key", "key": "GLFW_KEY_LEFT_SHIFT" },
    { "type": "key", "key": "GLFW_KEY_W" }
  ]
}
```

#### 文本输出
```json
// FCL
{
  "outputText": "/gamemode creative"
}

// ZL2
{
  "clickEvents": [
    {
      "type": "send_text",
      "key": "/gamemode creative"
    }
  ]
}
```

#### 视图组切换
```json
// FCL
{
  "bindViewGroup": ["group-id-1", "group-id-2"]
}

// ZL2
{
  "clickEvents": [
    { "type": "switch_layer", "key": "group-id-1" },
    { "type": "switch_layer", "key": "group-id-2" }
  ]
}
```

### 特殊属性

#### isPenetrable（触摸穿透）
```typescript
// FCL 的 pointerFollow 映射为 ZL2 的 isPenetrable
isPenetrable = fclButton.event.pointerFollow
```

用途：允许在按钮上移动虚拟鼠标指针（如鼠标左右键）

#### isToggleable（开关模式）
```typescript
// 当 autoKeep 为 true 且 autoClick 为 false 时启用
isToggleable = autoKeep && !autoClick
```

用途：点击后保持按下状态（如 Shift 潜行）

#### isSwipple（滑动联动）
```typescript
// 方向键按钮默认启用
isSwipple = true  // 对于方向键按钮
```

用途：手指滑动时触发相邻按钮

## 🔧 故障排除

### 问题1: 转换失败

**症状**: 点击转换后显示错误

**解决方案**:
1. 检查 JSON 格式是否正确
2. 确保是完整的 FCL 配置
3. 查看错误提示信息

### 问题2: 导入 ZL2 后崩溃

**症状**: ZL2 导入配置后应用崩溃

**原因**: 可能是颜色值问题

**解决方案**:
1. 使用转换器生成的配置（已使用安全颜色值）
2. 不要手动修改颜色值
3. 如需自定义，使用 ZL2 编辑器

### 问题3: 按钮位置不对

**症状**: 按钮位置与 FCL 不同

**原因**: 坐标系统差异

**解决方案**:
1. 在 ZL2 编辑器中调整位置
2. 检查参考尺寸（screen_width vs screen_height）

### 问题4: 按键不响应

**症状**: 按钮点击无反应

**原因**: 键码映射问题

**解决方案**:
1. 检查转换日志
2. 确认键码在映射表中
3. 手动在 ZL2 中配置按键

## 📊 转换统计

转换完成后会显示：
- 控件层数量
- 按钮数量
- 方向键数量
- 样式数量

这些信息帮助你验证转换是否完整。

## 💡 最佳实践

### 1. 转换前
- ✅ 备份原 FCL 配置
- ✅ 检查 JSON 格式
- ✅ 了解控件功能

### 2. 转换中
- ✅ 使用示例测试
- ✅ 查看转换统计
- ✅ 检查输出格式

### 3. 转换后
- ✅ 在 ZL2 中测试
- ✅ 调整位置和大小
- ✅ 验证所有按键功能
- ✅ 保存最终配置

## 🎨 自定义样式

如需自定义样式：

1. **使用 ZL2 编辑器**（推荐）
   - 打开 ZL2 控件编辑器
   - 使用可视化颜色选择器
   - 调整样式属性
   - 导出配置

2. **修改透明度**（安全）
   ```json
   {
     "alpha": 0.8,  // 80% 不透明度
     "pressedAlpha": 1.0
   }
   ```

3. **不要做的事**（危险）
   - ❌ 手动修改颜色值
   - ❌ 使用在线颜色转换工具
   - ❌ 复制其他项目的颜色值

## 📚 参考文档

- [FCL 控件系统开发文档](./fcl控件文档/FCL控件系统开发文档.md)
- [FCL 控件系统快速参考](./fcl控件文档/FCL控件系统快速参考.md)
- [ZL2 控件系统开发文档](./zl2控件文档/ZL2控件系统开发文档.md)
- [ZL2 颜色值安全指南](./zl2控件文档/快速参考-颜色值安全指南.md)
- [ZL2 实例控件说明](./zl2控件文档/实例控件说明.md)

## 🤝 技术支持

如遇问题：
1. 查看本指南的故障排除部分
2. 检查参考文档
3. 提交 Issue（附上错误信息和配置文件）

## 📝 更新日志

### v1.0.0 (2024)
- ✅ 初始版本
- ✅ 支持 FCL 到 ZL2 转换
- ✅ 键码自动映射
- ✅ 方向键转换
- ✅ 安全颜色值处理
- ✅ 响应式 UI

---

祝你使用愉快！🎮
